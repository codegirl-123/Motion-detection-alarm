# Motion-detection-alarm
This Arduino program checks for motion using a PIR sensor. When motion is detected, the built-in LED turns ON. When there is no motion, the LED stays OFF. The program continuously monitors the sensor for movement.
